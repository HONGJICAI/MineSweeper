{
  "name": "caiji-games",
  "private": true,
  "packageManager": "pnpm@10.13.0",
  "scripts": {
    "build": "pnpm -r --parallel run build",
    "build:shared": "pnpm --filter @caiji-games/* run build",
    "build:web": "pnpm build:shared && pnpm -r --parallel run build:web",
    "build:desktop": "pnpm build:shared && pnpm -r --parallel run build:desktop",
    "tauri": "pnpm build:shared && pnpm -r --workspace-concurrency=1 run tauri",
    "tauri:ms": "pnpm --filter minesweeper tauri:ms"
  }
}
