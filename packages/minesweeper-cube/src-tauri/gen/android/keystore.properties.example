# Copy to keystore.properties (which is gitignored) and fill in the passwords.
# The path can use forward slashes even on Windows.
storeFile=keystore.jks
storePassword=PUT_KEYSTORE_PASSWORD_HERE
keyAlias=caiji-release
keyPassword=PUT_KEY_PASSWORD_HERE
